{
  "name": "sass_live_streaming",
  "version": "1.0.0",
  "description": "landing page",
  "scripts": {
    "sass:watch": "sass --style=expanded --watch styles:dist",
    "parcel:watch": "parcel serve --dist-dir public",
    "parcel:build": "parcel build index.html --dist-dir build --public-url ./"
  },
  "author": "Dinis",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.15.4",
    "sass": "^1.89.2"
  }
}
